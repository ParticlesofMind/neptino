{
  "name": "css-selector-inspector",
  "version": "1.0.0",
  "description": "Chrome extension for inspecting and copying CSS selectors",
  "type": "module",
  "scripts": {
    "build": "npm run build:all && npm run copy-assets",
    "build:all": "npm run build:content-script && npm run build:background && npm run build:popup",
    "build:content-script": "ENTRY=content-script vite build",
    "build:background": "ENTRY=background vite build",
    "build:popup": "ENTRY=popup vite build",
    "copy-assets": "node scripts/copy-assets.js",
    "generate-icons": "node scripts/generate-icons.js",
    "watch": "npm run build:all -- --watch",
    "dev": "npm run watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "typescript": "^5.0.0",
    "vite": "^6.3.5"
  }
}

