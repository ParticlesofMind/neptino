#!/bin/bash
# =============================================================================
# JULES AI - NEPTINO SETUP (Copy this entire script to Jules)
# =============================================================================
set -e
cd /app
echo "🚀 Neptino Setup for Jules AI"
echo "================================"
npm ci --prefer-offline --no-audit 2>/dev/null || npm install
echo "✅ Dependencies installed"
npx playwright install chromium --with-deps || sudo npx playwright install-deps chromium && npx playwright install chromium
echo "✅ Playwright ready"
npm run build 2>/dev/null || echo "⚠️ Build skipped"
echo ""
echo "✅ Setup complete!"
echo "🎯 Ready to run: npm test"
